<%= simple_form_for @question, url: {action: "create"}, html: {class: "form new-form"} do |f| %>
  <%= render partial: 'form', locals: { f: f } %>

  <%= f.submit "Guardar y agregar preguntas", class: 'btn btn-primary'%>
<% end %>

<%= link_to 'Regresar', comprehension_questions_path, class: 'btn btn-primary mt-5'%>

