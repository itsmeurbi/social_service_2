<div class='container-fluid'>
  <div class='title'>
    <h2>Preguntas opción múltiple</h2>
    <%= link_to 'Agregar pregunta', new_multiple_question_path, class: 'btn btn-light' %>
  </div>
  <% @questions.each do |question| %>
    <div class='row container-q border-bottom'>
      <div class='col-9 question'><%= question.content %></div>
      <div class='col-3 opciones'>
        <%= link_to 'Ver', multiple_question_path(question)%>
        <%= link_to 'Editar', edit_multiple_question_path(question)%>
        <%= link_to 'Eliminar', multiple_question_path(question), method: :delete%>
      </div>
    </div>
  <% end %>
</div>
